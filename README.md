# C++ Tutorial

In order to build the project execute

```bash
cmake -S ./ -B out/build/
make -C out/build
```

The executable is built in the /bin folder as `test`

# Resource
This project is based on concepts from Data Structures and Algorithm Analysis.
