#ifndef TOKEN_PASTING_OPERATOR_H
#define TOKEN_PASTING_OPERATOR_H

#include <iostream>
#include <string>

using namespace std;

/*
 *   // Use the variable macro to create variables at runtime
 *   // Expands to: int user_id_alex = 1001;
 *   DEFINE_USER_ID(alex, 1001);
 *   
 *   // Expands to: int user_id_gloria = 2002;
 *   DEFINE_USER_ID(gloria, 2002);
 *
 *   // We can call these variables directly because they exist exactly like this in the 
 *   // code post-preprocessing
 *   std::cout << "Alex's Generated ID variable value: " << user_id_alex << "\n";
 *   std::cout << "Gloria's Generated ID variable value: " << user_id_gloria << "\n\n";
 *
 *   // Call the functions generated by our macro
 *   int sum = compute_add(15, 20);
 *   std::cout << "Result of compute_add: " << sum << "\n\n";
 *
 *   int product = compute_multiply(4, 5);
 *   std::cout << "Result of compute_multiply: " << product << "\n";
 */

// ============================================================================
// VARIABLE CREATION MACRO
// Concatenates "user_id_" with whatever name is provided to create a variable name.
// ============================================================================
#define DEFINE_USER_ID(name, id_value) int user_id_ ## name = id_value

// ============================================================================
// SYSTEM FUNCTION GENERATOR MACRO
// Creates a distinct function name using the function type parameter.
// ============================================================================
#define GENERATE_MATH_WRAPPER(func_name, math_operator)             \
    int compute_ ## func_name (int a, int b) {                      \
        cout << "Running custom " << #func_name << " function\n"; \
        return a math_operator b;                                   \
    }

// 1. Let the preprocessor build two distinct functions for us
// Generates: int compute_add(int a, int b) { ... }
GENERATE_MATH_WRAPPER(add, +)

// Generates: int compute_multiply(int a, int b) { ... }
GENERATE_MATH_WRAPPER(multiply, *)


#endif
